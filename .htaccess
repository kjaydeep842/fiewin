<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Handle Front Controller for Shared Hosting (InfinityFree / cPanel)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
